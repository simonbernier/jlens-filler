# Core
torch>=2.3
transformers>=4.57          # Qwen3.5 / DeepSeek-V4 architectures landed in recent transformers; bump if load fails
accelerate>=0.34            # device_map="auto" sharding across GPUs
huggingface_hub>=0.25       # JacobianLens.from_pretrained downloads lens .pt from the Hub
safetensors>=0.4

# Quantization
kernels>=0.16,<0.17         # DeepSeek V4 Flash: transformers runs its FP8 block-quantized linears
                            # through the Hub-hosted Triton kernel kernels-community/finegrained-fp8,
                            # fetched by this package on first forward (range pinned by transformers 5.16)
bitsandbytes>=0.43          # 4-bit / 8-bit weight loading for bf16 side models (gemma-27b); NOT DeepSeek

# API access (stage 1: 10/11 via OpenRouter)
python-dotenv>=1.0          # api_common loads OPENROUTER_API_KEY from a local .env

# Analysis / plotting
numpy>=1.26
pandas>=2.0
matplotlib>=3.8
tqdm>=4.66              # progress bars for API sweeps (10/11)

# The Jacobian-lens library itself is installed from git in setup_env.sh:
#   pip install -e ./jacobian-lens
# (package name: jlens). It is a reference implementation, not on PyPI.
